1. jeżeli rozmiar tablicy będzie ustawiony na stałe bez możliwości jej zmiany utworzona struktura będzie tablicą statyczną
2. deklaracja i wypełnianie tablicy inicjalizacja:
      - int tablica[5]={4,5,6,2,3}
      - lub nawet nie trzeba podawać rozmiaru
        : int tablica[]={4,5,6,2,3}
        : char tablica[5]={'z','i','k','i','t'}
3. generowanie liczb pseudolosowych: do generowanie liczb losowych służy funkcja rand(). Generacja generuje ona losową
  liczbę całkowitą między 0 i RANDD_MAX (stała zdefiniowana w cstdlib)
4. funkcje - zalety dzielenia programu na podprogramy
      - dany podprogram można wywołać kilka razy, zmniejszamy liczbę linijek kodu;
      - program łatwiej zrozumieć 
    Programowanie proceduralne to dzielenie kodu na programy
5. Zmienne to obszar w pamięci komputera której możemy przepisywać różne wartości.
   Deklaracja zmiennej to rezerwacja pamięci dnia zmiennej w pamięci komputera.
   int a; //deklaracja
   ing b=7;//inicjalizacja
   Kompilator nie zeruje zadeklarowanych zmiennych powyżej zadeklarowana zmienna 'a' otrzymana przypadkowo wartość
6. Operator trojargumentowy '?' zwraca wartość w zależności od warunku. Przykład użycia:
    int a=3<5?2:1; //zmienna a przyjmuje wartość 2 bo warunek 3 < 5 jest prawdziwy
    cout << (a%2==1?"liczba jest nieparzysta":"liczba jest parzysta");
7. funkcje matematyczne:
   floor() - funkcja wzór zaokrągla liczby zawsze w dół gdy w liczbie występuje część ułamkowa.
   ceil() - funkcje ceil zaokrągła liczby zawsze w górę gdy w liczbę występuje część ułamkowa.
   round() - naturalne zaokrąglenie liczby. Як в математиці.
   abs() - zwraca wartość bezwzględną liczby, przekazany jako argument funkcji.
   sqrt() - funkcja oblicza pierwiastek stopnia drugiego. Ppierwszym i jednym argumentem tej funkcji jest liczba z której chcemy policzyć pierwiastek.
   pow() - podnoszenie liczby do dowolnej potęgi funkcja ta przyjmuje dwa argumenty i wracał wynik potęgowania. 
          Pierwszym argumentem jest liczba którą chcemy potęgować drugim natomiast potęga do których chcemy wybraną liczbę w podnieść.